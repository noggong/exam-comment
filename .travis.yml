sudo: required

language: generic

services:
  - docker

before_install:
  - echo "starting Creating an image with dockerfile"
  - docker build -t noggong/exam-comment -f Dockerfile.dev .

script:
  - docker run -e CI=true noggong/exam-comment yarn test -- coverage

  after_success:
  - echo "Test success"
